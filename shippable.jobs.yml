jobs:      
  - name: dv-run-sh
    type: runSh
    steps:
      - IN: dv-params-1
      - TASK:
        - script: terraform --version
        - script: kubectl version || true
        - script: jfrog --version
        - script: gcloud --version
        - script: eb --version
        - script: packer --version
        - script: echo "before azure"
        - script: apt search azure-cli
        - script: az --version
        - script: echo "after azure"
        - script: yarn --version
        
        
